import 'package:flutter/material.dart';

class PhoneLoginView extends StatelessWidget {
  const PhoneLoginView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Phone Login UI here"));
  }
}
